# zhihuya188.github.io Hugo 站点管理指南

> 适用仓库：`https://github.com/zhihuya188/zhihuya188.github.io`
> 技术栈：Hugo v0.160.1 (extended) + Stack 主题 + GitHub Pages（GitHub Actions 自动部署）

---

## 1. 站点结构（两个分支，各司其职）

| 分支 | 内容 | 作用 |
|------|------|------|
| `main` | Hugo **源码**（`config.toml`、`content/`、`themes/`、`.github/workflows/hugo.yml`） | **日常管理的唯一分支**：改文章、改配置都在这里 |
| `master` | 2019 年手工构建的旧静态文件（`index.html`、`post/`…） | 旧版产物，**已弃用**。部署改由 GitHub Actions 接管后，此分支不再更新，可忽略（如需清理，见第 7 节） |

### 源码目录速览

| 路径 | 说明 |
|------|------|
| `config.toml` | 站点配置：标题、描述、菜单、社交链接、主题 |
| `content/posts/` | 文章目录（Markdown 文件） |
| `content/about.md` | 关于页 |
| `themes/stack/` | 主题（git submodule，不直接改里面的文件） |
| `.github/workflows/hugo.yml` | 自动构建 + 部署工作流 |
| `.tools/hugo` | 本地 Hugo 二进制（已被 gitignore，只在本机用） |
| `public/` | 本地构建输出（被 gitignore，不提交） |

---

## 2. 本地环境（本机已配置好）

```bash
# 主题子模块
git submodule update --init --recursive

# 本地 Hugo（版本与线上一致：v0.160.1 extended）
.tools/hugo version
```

> 若 `.tools/hugo` 丢失，重新下载：
> ```bash
> mkdir -p .tools && cd .tools
> wget -q https://github.com/gohugoio/hugo/releases/download/v0.160.1/hugo_extended_0.160.1_linux-amd64.tar.gz -O hugo.tar.gz
> tar xzf hugo.tar.gz && rm hugo.tar.gz
> ```

---

## 3. 日常流程：写一篇文章并发布

### 3.1 新建文章

```bash
cd /home/yangyang/hugo_zhihuya188
git pull origin main          # 先同步最新
.tools/hugo new posts/我的新文章.md
```

生成的文件开头有 front matter：

```yaml
---
title: "我的新文章"
date: 2025-08-15
draft: true
---
```

### 3.2 本地预览

```bash
.tools/hugo server -D
```

- `-D` 表示同时显示草稿（draft: true 的文章）
- 浏览器打开 `http://localhost:1313` 实时预览
- 按 `Ctrl+C` 退出

### 3.3 发布

```bash
# 1. 把文章开头 draft 改为 false（或删除该行）
# 2. 提交推送（推送 main 即触发自动部署）
git add -A
git commit -m "新增文章：我的新文章"
git push origin main
```

等 GitHub Actions 跑完（约 1~2 分钟），访问 `https://zhihuya188.github.io` 即可看到更新。

---

## 4. 部署机制（无需手动操作）

- 工作流文件：`.github/workflows/hugo.yml`
- 触发条件：**推送 `main` 分支**，或仓库 Actions 页面手动 "Run workflow"
- 构建命令：`hugo --gc --minify`（与本地一致）
- 要求仓库 **Settings → Pages → Source 选择 "GitHub Actions"**（新仓库默认如此）
- 构建时 `configure-pages` 自动注入正确的 baseURL，`config.toml` 里的 baseURL 只在本地预览时生效

---

## 5. 常见管理任务

### 5.1 改站点名称 / 描述
编辑 `config.toml`：
```toml
title = '我的 Hugo 站点'
[params]
  description = '一句话描述'
```

### 5.2 改顶部菜单 / 页脚社交链接
- 菜单：`config.toml` 的 `[menu]` 段
- 社交图标：`[params.socialIcons]` 段（github / rss 等）

### 5.3 更新 Stack 主题
```bash
git submodule update --remote themes/stack
git add themes/stack
git commit -m "update theme"
git push origin main
```

### 5.4 加图片
- 全局图片：放 `static/` 目录（若不存在则新建），引用路径如 `/images/xxx.png`
- 单篇文章图片：放 `content/posts/` 同目录，Markdown 里用相对路径引用

### 5.5 删除文章
删掉 `content/posts/xxx.md` 后提交推送即可。

---

## 6. 故障排查

| 现象 | 原因与处理 |
|------|-----------|
| 推送后站点没更新 | 打开仓库 **Actions** 页看构建是否失败；失败信息一般在日志末尾 |
| 构建失败：`Error: Error building site` | 多为 Markdown 语法或 front matter 问题，本地跑 `hugo` 复现并修复 |
| 页面 404 | 检查 `baseURL` 是否是 `https://zhihuya188.github.io/`；或文件是否真的 push 到了 main |
| 本地能预览但线上样式乱 | 确认线上用的是 GitHub Actions 部署（不是旧 master 分支） |
| `.tools/hugo` 报版本不兼容 | 重新下载 v0.160.1 二进制（见第 2 节） |

---

## 7. 可选清理：废弃的 master 分支

master 分支只有 2019 年的一次构建产物，不再需要。若想删除（需先确认 Pages 部署不依赖它——检查 Settings → Pages → Source 是否为 "GitHub Actions"）：

```bash
git push origin --delete master
```

> 删除前请确认 Actions 部署正常，否则线上站点会 404。

---

## 8. 常用命令速查

```bash
.tools/hugo server -D              # 本地预览（含草稿）
.tools/hugo new posts/xxx.md       # 新建文章
.tools/hugo --gc --minify          # 手动构建到 public/
git pull origin main               # 同步远端
git add -A && git commit -m "msg"  # 提交
git push origin main               # 发布（触发自动部署）
```
